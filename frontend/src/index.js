import React from 'react';
import ReactDOM from 'react-dom';
import './app.css'; // Optional CSS if you want to style your app
import App from './app';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
